#!/usr/bin/env python

import unittest
from Bridge import Bridge
from Packet import Packet
from ForwardingTable import ForwardingTable


class TestBridgeMethods(unittest.TestCase):
    #testBridge1 = Bridge(1)
    # _create_sockets_for_lans(self, LAN_list):


if __name__ == '__main__':
    unittest.main()
